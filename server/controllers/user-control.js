const User = require('../models/users')
const validator = require('validator')

createUser = (req, res) => {
    
}
module.exports = {
    createGame,
    updateGame,
    deleteGame,
    getGames,
    getGameById,
}